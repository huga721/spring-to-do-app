spring:
  h2.console:
    enabled: true
    path: '/console'
  datasource:
    url: 'jdbc:h2:file:./todo.db'
  jpa:
    hibernate:
      ddl-auto: validate # now hibernate gonna validate data in sql db


